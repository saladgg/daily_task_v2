export interface Task {
  taskId: number;
  isComplete: boolean;
  dayTask: string;
  taskTime: string;
}
/* export class Task{
      constructor(
          public dayTask: String,
          public taskTime: String,
          public status: Boolean
      ) { }
  } */
